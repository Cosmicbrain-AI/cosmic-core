# Copy to .env.local and fill in. Public values (VITE_*) may ship to the browser.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Server-only secrets (no VITE_ prefix):
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
